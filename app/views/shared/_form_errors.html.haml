- if resource.errors.any?
  .panel.panel-danger
    .panel-heading
      %h3.panel-title= pluralize(resource.errors.count, "error")
    .panel-body
      %ul
        - resource.errors.full_messages.each do |message|
          %li.text-danger= message
