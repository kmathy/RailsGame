<%= form_for(@tournament_user) do |f| %>
  <% if @tournament_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tournament_user.errors.count, "error") %> prohibited this tournament_user from being saved:</h2>

      <ul>
      <% @tournament_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
